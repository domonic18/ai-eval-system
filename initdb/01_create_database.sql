CREATE DATABASE IF NOT EXISTS ai_eval 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

CREATE USER '${MYSQL_USER}'@'%' IDENTIFIED BY '${MYSQL_PASSWORD}';
GRANT ALL PRIVILEGES ON ai_eval.* TO '${MYSQL_USER}'@'%';

FLUSH PRIVILEGES;